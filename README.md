# Algorytmy

```
git add -A      # dodaje wszystkie pliki
git status -s    #wyświeta status plików tzn czy dodane
git commit -m "wiadmosc o zmianach" #rozi commit-a tzn paczke ze zmianami
git push   #wrzuca na server
```